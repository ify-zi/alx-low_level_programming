#include "main.h"
#include <string.h>

/**
* *_strncpy - copies a strong
* @dest: destinat9on
* @src: source
* @n: colecr sam integer
*
* Return: return an integer
*/

char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
