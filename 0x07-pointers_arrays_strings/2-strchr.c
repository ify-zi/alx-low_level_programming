#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
* *_strchr - searches fir a character
* @s: string to searchbin
* @c: the character to find
*
* Return: return a value
*/

char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
