#include "main.h"
#include <string.h>

/**
* _strlen - returns the lenght of the string
* @s: the collector of the arg
*
* Return: return the length of the string
*/

int _strlen(char *s)
{
	return (strlen(s));
}
