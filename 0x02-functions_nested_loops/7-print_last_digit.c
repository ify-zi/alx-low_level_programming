#include "main.h"

/**
* print_last_digit - obtains the last digit ina number
* @n: operand
*
* Return: 0
*/

int print_last_digit(int n)
{
	m = n % 10;
	if (n < 0)
	{
		ld = -m;
		return (ld)(ld);
	}
	return (m)(m);
}
